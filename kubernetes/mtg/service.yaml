apiVersion: v1
kind: Service
metadata:
  name: mtg
  namespace: mtg-proxy
  labels:
    app: mtg
spec:
  type: LoadBalancer
  ports:
    - port: 4433
      targetPort: 3128
      protocol: TCP
      name: mtproto
  selector:
    app: mtg