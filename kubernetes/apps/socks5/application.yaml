apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: socks5
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/sacrede-mgn/home-infra.git'
    targetRevision: HEAD
    path: kubernetes/socks5
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: socks5-proxy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
