apiVersion: v1
kind: Service
metadata:
  name: mtproxy-svc
  namespace: mtproxy
spec:
  type: NodePort
  selector:
    app: mtproxy
  ports:
  - port: 8443
    targetPort: 8443
    nodePort: 31443
    protocol: TCP
