---
- name: Install sudo on Debian-based servers
  hosts: all
  become: true
  become_method: su
  become_flags: "-"
  tasks:
    - name: sacrede to sudoers
      community.general.sudoers:
        name: sacrede to sudoers
        state: present
        user: sacrede
        commands: ALL